import SearchClient from './SearchClient';

export const metadata = {
    title: 'Search | The Golf Press',
    description: 'Search for articles, news, and more on The Golf Press.',
};

export default function SearchPage() {
    return <SearchClient />;
}
